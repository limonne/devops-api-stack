apiVersion: v1
kind: Pod

metadata:
  name: nginx-toleration

spec:

  tolerations:

  - key: dedicated
    operator: Equal
    value: database
    effect: NoSchedule

  containers:

  - name: nginx

    image: nginx
